// import { getAuth } from "@firebase/auth";
// import index from "@/firebase";

// jest.mock("@firebase/auth");

// describe("Auth Module", () => {
//   it("should initialize auth with the Firebase app", () => {
//     require('@/firebase/auth.ts'); // Lazy load to ensure mocks are set before the module
//     expect(getAuth).toHaveBeenCalledWith(index);
//   });
// });
