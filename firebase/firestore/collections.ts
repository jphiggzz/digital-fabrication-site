// name of the time slots collection in Firebase
export const TIME_SLOTS_COLLECTION = 'timeSlots';

// name of the reservation collection in Firebase
export const RESERVATION_COLLECTION = 'reservations';

// name of the printer collection in Firebase
export const PRINTER_COLLECTION = 'printers';